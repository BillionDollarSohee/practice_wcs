<Window x:Class="TaskPractice.WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:TaskPractice.WPF.ViewModels"
        Title="대차 입고 시뮬레이터" Height="700" Width="1000">

    <Window.DataContext>
        <vm:MainWindowViewModel/>
    </Window.DataContext>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="200"/>
        </Grid.RowDefinitions>

        <!-- 타이틀 -->
        <TextBlock Grid.Row="0" Text="대차 입고 시뮬레이터"
                   FontSize="20" FontWeight="Bold" Margin="0,0,0,10"/>

        <!-- 입고 시작 컨트롤 -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="출발지 : " VerticalAlignment="Center"/>
            <ComboBox Width="100" Margin="5,0"
                      ItemsSource="{Binding FromNodeIds}"
                      SelectedItem="{Binding SelectedFromId}"/>

            <TextBlock Text="도착지 : " VerticalAlignment="Center"/>
            <ComboBox Width="100" Margin="5,0"
                      ItemsSource="{Binding ToNodeIds}"
                      SelectedItem="{Binding SelectedToId}"/>

            <Button Content="입고 시작" Width="80" Margin="10,0,0,0"
                    Command="{Binding StartCommand}"/>
        </StackPanel>

        <!-- 중간 영역 : 컨베이어 상태 + 오더 현황 -->
        <Grid Grid.Row="2" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- 컨베이어 노드 상태 -->
            <GroupBox Grid.Column="0" Header="컨베이어 상태" Margin="0,0,5,0">
                <ItemsControl ItemsSource="{Binding ConveyorNodes}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Margin="5" Width="60" HorizontalAlignment="Center">
                                <Ellipse Width="30" Height="30" HorizontalAlignment="Center">
                                    <Ellipse.Style>
                                        <Style TargetType="Ellipse">
                                            <Setter Property="Fill" Value="LightGray"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsActive}" Value="True">
                                                    <Setter Property="Fill" Value="LimeGreen"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Ellipse.Style>
                                </Ellipse>
                                <TextBlock Text="{Binding NodeId}"
                                           FontSize="10"
                                           HorizontalAlignment="Center"/>
                            </StackPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </GroupBox>

            <!-- 오더 현황 -->
            <GroupBox Grid.Column="1" Header="오더 현황" Margin="5,0,0,0">
                <DataGrid ItemsSource="{Binding Orders}"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          CanUserAddRows="False">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="OrderId" Binding="{Binding OrderId}" Width="80"/>
                        <DataGridTextColumn Header="CartId" Binding="{Binding CartId}" Width="80"/>
                        <DataGridTextColumn Header="출발" Binding="{Binding FromId}" Width="60"/>
                        <DataGridTextColumn Header="도착" Binding="{Binding ToEqpId}" Width="60"/>
                        <DataGridTextColumn Header="상태" Binding="{Binding Status}" Width="80"/>
                    </DataGrid.Columns>
                </DataGrid>
            </GroupBox>
        </Grid>

        <!-- 로그 -->
        <GroupBox Grid.Row="3" Header="로그">
            <ListBox ItemsSource="{Binding Logs}"/>
        </GroupBox>
    </Grid>
</Window>